# TODO

---

#### feed

prompt: ok using our "get_activity_feed()" and our "get_account_id_post()" lets create a new infinit scroll feed widget, we can get in small batches and then auto pagniate as the user scorls creat the widget file in the wiget flder tehn import and use on the feed page
this shpuld be a clean implenation with clean functions for fecthing and auto pagnation

- [ ] add or use post type
- [ ] what i mean by clean implentaion i mean use the "get_activity_feed()" in a clean way that we can pass diffent options to it form the FeedOptions
- [ ] create a post comonent/widfget that fectehs its own post

updated promt: the above was written a while a go we have some of this implented i think, but not the feed options, like descening or aasending. reasearch the suppored options for the getactivity feed to we can pass those to our function and implnet our feed options in a clean way, i can't remember if we started anything, but maybe lets save feed options to loacl storge and use them when we fecth the feed, there should be a type interface for these options



---

### other
- add a like button, that sends like tx when user clicks, and also shows the tolal likees
- add a comment button, that shows comment count, add the abitly to comment afetr we add the compose functionaity for new post
- add a post route feed/post or profile/accountid/post? seince we speficy post by block height, the post page should have comments
- add a repost button
- fix: line break for really long account adress on /profile/accountid page
- 

==============================================
<br/>
copyright 2026 by sleet.near
