// ============================================
const ROUTES = {
	home: { path: '/', label: 'HOME' },
	profile: { path: '/profile', label: 'PROFILE' }
} as const;

export default ROUTES;
// ============================================
